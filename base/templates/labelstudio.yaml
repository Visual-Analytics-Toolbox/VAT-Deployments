apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: labelstudio
spec:
  project: default
  source:
    repoURL: 'git@github.com:Visual-Analytics-Toolbox/VAT-Deployments.git'
    path: labelstudio
    targetRevision: HEAD
    helm:
      releaseName: labelstudio
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: labelstudio # has to be there because thats where the base application is
  syncPolicy:
    automated: {}
    syncOptions:
      - CreateNamespace=true
