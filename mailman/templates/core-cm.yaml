apiVersion: v1
kind: ConfigMap
metadata:
  name: mailman-core-config
  namespace: mailman
data:
  mailman.cfg: |
    [mta]
    lmtp_host: 0.0.0.0
    lmtp_port: 8024

    [webservice]
    # This ensures the API listens for the Web pod
    hostname: 0.0.0.0
    port: 8001
