apiVersion: v1
kind: Service
metadata:
  name: ffmpeg-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: ffmpeg
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: ffmpeg